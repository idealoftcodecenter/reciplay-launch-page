const ArrowNext = () => {
    return (
        <span className="w-[40px] h-[40px] flex items-center justify-center group">
            <span className="inline-block w-[18px] h-[2px] bg-black transition-all duration-500 group-hover:w-[32px] relative after:w-[12px] after:h-[12px] after:block after:border-t-2 after:border-r-2 after:border-black after:absolute after:right-0 after:top-[-5px] after:rotate-45"></span>
        </span>
    );
}

export default ArrowNext;
