/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    distDir: "dist",
    basePath: '/demo/reciplay-launch-page',
    assetPrefix: ""
};

export default nextConfig;
